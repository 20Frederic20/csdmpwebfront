services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend_csdmp
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next